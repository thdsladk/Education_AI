using UnityEngine;

public class ForEachExample : MonoBehaviour
{
    void Start()
    {
        // 문자열 : 문자를 순서대로 나'열' ( 문자 배열 ) 
        
        // foreach 배열이나 컬렉션의 요소를 처음부터 끝까지 순서대로 간편하게 접근할 때 사용하는 반복문 
        //foreach (char c in myName)
        //{
        //    Debug.Log(c);
        //}
    }
    
    void Update()
    {
        
    }
}
